{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // -----------------------------------------------------------------------
  // Pipeline backend selector.
  // Supported values: "Fast" | "WhatsAppTeam"
  // Override per-environment: set env var  PipelineMode=WhatsAppTeam
  // -----------------------------------------------------------------------
  "PipelineMode": "Fast",
  // -----------------------------------------------------------------------
  // Configuration for the FastPipelineBackend (Python FastAPI service).
  // Override via env var: PythonPipeline__BaseUrl=http://prod-host:8001
  // -----------------------------------------------------------------------
  "PythonPipeline": {
    "BaseUrl": "https://stages-nikon-clubs-departments.trycloudflare.com"
  },
  // -----------------------------------------------------------------------
  // (Placeholder — will be used by WhatsAppTeamBackend once integrated)
  // "WhatsAppPipeline": { "BaseUrl": "https://api.whatsapp-team.example.com" }
  // -----------------------------------------------------------------------
  "SttService": {
    "BaseUrl": "https://stages-nikon-clubs-departments.trycloudflare.com"
  }
}